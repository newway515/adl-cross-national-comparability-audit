# 下一步做什么

> **当前状态：** M1-M4 全管线完成。所有分析结果就位。论文写作可随时开始。
> **最后更新：** 2026-07-13

---

## 一、课题已完成的工作（无需重做）

- [x] **M1 数据抽取**：13 人群 × 5 ADL 条目，64,644 人，已抽取并审计。数据在 `D:\cursorproj\cursorEvaluation\cur260711\analysis\data\*.pkl`
- [x] **M2 操作化**：CHARLS 原始 4 级量表提取，E2=16.1pp，全 13 人群 2PL IRT 参数估计完成。结果在 `HANDOFF_FINAL/04_分析结果/m2_operationalization_results.json`
- [x] **M3 DIF 分解**：78 国家对的 4 种识别结构下 DIF 包络全部完成（312/312 收敛）。结果在 `HANDOFF_FINAL/04_分析结果/m3_pairwise_checkpoint.json`
- [x] **M4 方差**：真 bootstrap R=50 × 12/13 人群（MHAS 除外）完成。
- [x] **识别边界图 + 可比性标记**：论文就绪数据在 `HANDOFF_FINAL/04_分析结果/V9_paper_frontier_data.json`
- [x] **PSU/strata 补齐**：CHARLS (446 PSU)/LASI (6052)/MHAS (7809)
- [x] **V9.1 研究方案 + GitHub/Zenodo 解盲前冻结**
- [x] **四份同行评审 + 评审对照表**
- [x] **Chan 2012 全文对照 + 第二轮文献碰撞矩阵**

## 二、论文写作：需要做的

### 优先级 1：立即可以做

1. **按 `05_论文材料/论文建议结构.md` 的大纲开始写论文正文**
   - 引言、方法、结果可以直接从 V9.1 方案和识别边界图数据中提取
   - 结果节的所有数字在 `05_论文材料/论文核心数字卡片.md` 中

2. **在写引言前，先做 Chan 2012 对照表的确证**
   - 已做的：Chan 2012 全文对照已完成（见 `02_评审与演变/Chan2012_全文逐行对照报告.md`）
   - 还需做的：与 Chan 2012 的结果节逐条句子对照，确保引言中的 gap statement 经得起逐字推敲

### 优先级 2：写完初稿后补

3. **非参数 DIF 三角验证**（代码尚未写）
   - Mantel-Haenszel + 逻辑回归 DIF 检验
   - 给 IRT 包络加一道独立证据链，防止审稿人攻击"你只在似然面内做文章"
   - 可以用这个作为结果节的稳健性附录

4. **LD 诊断**（代码尚未写）
   - Yen's Q3 残差相关矩阵
   - 确认 ADL 条目的局部依赖是否污染了 E3 包络
   - 可以作为敏感性分析的附录

5. **mirt 交叉验证**（代码尚未写）
   - 用 R 的 mirt 包在 HRS 上跑 2PL，与自实现对比参数估计的一致性
   - 一次性的验证，论文方法节的一句"自实现与 mirt 等价设定的参数一致性 r > 0.99"

### 优先级 3：投稿前

6. **逐条核验所有"待核证"的 DOI**（共约 15 篇文献）
   - 通过 PubMed/Crossref 验证每篇存在、DOI 正确、引用信息完整
   - 这是 CLAUDE.md 的红线——不得臆造 DOI

7. **完成会议摘要/博士论文/预印本的补充检索**
   - 确认没有灰色文献中的未发表先例

## 三、如果还需要跑分析代码

### 快速重新运行关键分析

```bash
# 所有代码在 HANDOFF_FINAL/03_分析代码/
# Python 环境: D:\miniconda\envs\myenv\python.exe

# 冒烟测试（可选，验证环境正常）
python 03_分析代码/smoke_charls_2pl.py

# M2 操作化 + 全人群 2PL（如果有新数据）
python 03_分析代码/adl_operationalizations.py

# M3 成对 DIF 分解（如果 M3 结果需要重新产生）
python 03_分析代码/run_m3_pairwise.py

# 生成论文就绪数据（识别边界图、可比性标记、全景统计）
python 03_分析代码/finalize_V9_deliverables.py

# 重新补齐 PSU/strata（仅当 M1 数据被覆盖时）
python 03_分析代码/backfill_psu_strata.py
```

### 如果要在新机器上复现

1. 确保 Python 环境安装了 `numpy`, `scipy`, `pandas`（不需要第三方 IRT 包）
2. M1 抽取层数据需要在 `D:\cursorproj\cursorEvaluation\cur260711\analysis\data\` 或修改脚本中的路径
3. 原始 SQLite 数据库在 `D:\clinicdatabase\SQLitedatabase\`
4. 所有分析脚本可以从 `03_分析代码/` 目录直接复制使用——它们是自包含的（函数都 inline 了）

## 四、论文完成后

- [ ] 将论文方法节转写为英文（如果需要英文投稿）
- [ ] 将补充材料（F1-F17 冻结规则 + C1-C10 审计报告）整理为期刊格式的附录
- [ ] 在 GitHub 上创建 v2.0.0 release，包含论文全文 + 最终代码
- [ ] 论文接收后，将代码、数据和补充材料通过 Zenodo 归档为正式引用版本

---

*需要更多上下文时，打开 `HANDOFF_FINAL/README.md` 查看完整交接包结构。*
