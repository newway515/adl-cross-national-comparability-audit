# 交接文件夹索引

本文件夹是「跨国 ADL 失能测量分解研究」的项目交接包。给完全没有上下文的接手人。

## 阅读顺序

1. **先读 `HANDOFF.md`** —— 唯一主文件。一份读完即可接手：课题定义、变更思路、当前任务、已完成、卡点、下一步、必避的坑。
2. 需要看真实数字时，看本文件夹内的三个 CSV（下方说明）。
3. 需要动手写代码时，回到工作空间根目录 `D:\cursorproj\curEvaluation\`，按 `HANDOFF.md` 第 6/8 节操作。

## 本文件夹内容

| 文件 | 说明 |
|---|---|
| `HANDOFF.md` | **主交接文件**，从零接手看这一份 |
| `M1_extract_audit.csv` | M1 数据层抽取审计：13 人群 N / 患病率 / 缺失 / 各条目阳性数（真实数据，已完成） |
| `C8_full_results.csv` | C8 模拟前置闸门结果（**合成数据**，非真实估计，仅证明算法可信） |
| `C8_H2_results.csv` | C8 的 H2 假设收缩敏感性结果（合成数据） |

## 重要提醒（详见 HANDOFF.md 第 7 节）

- `C8_*.csv` 里的数字**全是合成数据**，不是真实世界结果，别混淆。
- 真实分析结果**尚未产出**，管线卡在 M2（见 HANDOFF.md 第 5 节的方法学决策点）。
- 完整的代码、方案文件、C1–C10 报告都在工作空间**根目录**，不在本文件夹（本夹只放交接必需的最小集）。

## 工作空间根目录的关键文件（不在本夹，需回根目录看）

- `adl_data_layer.py` —— M1 数据层，已完成可运行
- `C8_full_run.py` —— 含 M3 要复用的 IRT 算法（est_bmu 等）
- `V8.1_方向决定书与OSF预注册冻结清单.md` —— 主纲领，F1–F14 规则 + 13 人群字段速查
- `V8.1_C1` … `V8.1_C10` —— 十份前置审计报告
- `analysis/data/*.pkl` —— 13 人群抽取数据（pickle 格式）

交接日期：2026-07-12
